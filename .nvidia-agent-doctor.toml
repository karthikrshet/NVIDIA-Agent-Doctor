# NVIDIA Agent Doctor example configuration.
# This schema contains only settings actively used by CLI commands.

[benchmark]
# Benchmarks never run as part of `nad doctor`; these only set safe defaults
# for an explicitly requested `nad benchmark run` command.
max_memory_mb = 128
timeout_seconds = 15

[mcp]
config_paths = []

[skills]
scan_depth = 3
